const config = require("./config");
const constants = require("./constants");
const packageJsonConfig = require("./packageJsonConfig.json");

module.exports = {
  config,
  constants,
  packageJsonConfig
};
